runtime: python311
instance_class: F2
service: python-chatbot

entrypoint: gunicorn -k uvicorn.workers.UvicornWorker -w 1 --timeout 120 main:app

automatic_scaling:
  max_instances: 2
  target_cpu_utilization: 0.65

inbound_services:
  - warmup

includes:
  - env_variables.yaml
